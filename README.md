# SPRITE-Processing-Pipeline
The analysis pipeline for SPRITE (Split-Pool Recognition of Interactions by Tag Extension) profiling assays, processes raw FASTQ data through sequential steps such as identifying barcodes, genome alignment, discard alignments, etc, and it supports multiple samples.
